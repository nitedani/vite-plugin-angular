{
  "name": "@nitedani/vite-plugin-angular",
  "version": "0.0.4",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "release": "pnpm build && cd vite-plugin-angular/ && pnpm release"
  },
  "devDependencies": {
    "@brillout/release-me": "^0.0.7",
    "turbo": "^1.4.6"
  },
  "engines": {}
}
